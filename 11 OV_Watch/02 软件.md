# 软件架构总览
![[Pasted image 20260818231420.png]]

----------------------
### 独立Bootloader的好处:
1. **极致安全的升级体验：** Bootloader和APP存放在Flash的不同分区，升级时只会擦写APP分区。就算升级失败，Bootloader依然完好，可以重新进入升级模式，或者启动上一个可用版本，彻底告别变砖风险。
2. **支持OTA空中升级:** 可以通过蓝牙把升级包传到手表里，Bootloader会自动完成升级流程，给手表更新功能和修复bug。
3. **开发维护更高效：** Bootloader只负责基础启动和升级，功能固定，一次开发长期稳定。APP主程序可以快速迭代，不用再担心改动影响系统启动，大大提升开发效率。
4. **系统扩展性更强:** 可以轻松添加开机密码、硬件健康检查、多版本系统切换等高级功能，甚至可以实现双系统启动，极大提升产品的可玩性和安全性。

### 启动流程：
1 开机运行Bootloader，初始化基础硬件
2 检查升级标志位，判断是否需要升级
3 如果不需要升级，就跳转到APP程序地址
4 APP启动，进入正常使用界面

### 内存分区
**BOOT区后面划分了一个Flag区，用于记录是否是完整的APP，这个位置是APP传输完成后才记录的，为的是保证程序完整性**
![[Pasted image 20260818232932.png]]
**0x08000000:** Bootloader的起始地址，从这里开始存放启动引导程序，一直到0x08007FFF，总共占用32KB空间
**0x08008000:** 升级标志位区域，用来存储升级状态信息，告诉Bootloader要不要执行升级
**0x0800C000:** APP主程序的起始地址，剩下的大部分空间都留给主程序，最大到0x08007FFFF

### 单片机的内存
**Flash闪存：嵌入式的"硬盘"**
✅ 断电不丢失：程序和数据永久保存
✅ 用来存储：Bootloader、APP程序、固件、永久配置参数
✅ 读取速度快，擦写次数有限：一般可以擦写10000次以上
✅ 我们聊的内存分区，就是在Flash里面划分不同区域
 
**RAM内存：嵌入式的"内存条"** 
⚡ 断电即清空：只存储运行时的临时数据
⚡ 用来存储：全局变量、堆栈、程序运行时的数据
⚡ 读写速度极快，但容量一般比Flash小
⚡ 不需要分区，由操作系统自动管理
 
**程序完整运行流程** 
1. 程序烧录 → Flash分区存储
2. 开机启动 → CPU从Flash加载Bootloader
3. 初始化完成 → 跳转到Flash中的APP地址
4. 程序运行 → 数据和变量存储在RAM中
---------------
# MDK架构
**MDK工程结构不是完全对应Software文件夹中的文件结构的** 
``` 
├─Application/MDK-ARM               # 用于存放.s文件
├─Application/User/Core             # 用于存放CubeMX生成的初始化文件
│  │  main.c
│  │  gpio.c
│  │  ...
│
├─Application/User/System           # 用于存放自定义的delay.c sys.h等
│  │  delay.c
│  │  ...
│
├─Application/User/Tasks            # 用于存放任务线程的函数
│  │  user_TaskInit.c
│  │  user_HardwareInitTask.c
│  │  user_RunModeTasks.c
│  │  ...
│
├─Application/User/MidFunc          # 用于存放管理函数
│  │  StrCalculate.c
│  │  HWDataAccess.c
│  │  PageManager.c
│
├─Application/User/GUI_APP          # 用于存放用户的ui app
│  │  ui.c
│  │  ...
│
├─Application/User/GUI_FONT_IMG     # 用于存放字体和图片
│  │  ...
│
├─Drivers/CMSIS
│  │  ...
│
├─Drivers/User/BSP                  # 用于存放板载设备驱动
│  │  ...
│
├─Middleware/FreeRTOS               # FreeRTOS的底层
│  │  ...
│
├─Middleware/LVGL/GUI               # LVGL的底层
│  │  ...
│
└─Middleware/LVGL/GUI_Port          # 用于存放LVGL驱动
    ├─lv_port_disp.c
    ├─lv_port_indev.c
```
------------------
### 简述一下各个片上外设的用途：
1. **DMA+SPI刷屏** ，SPI通信不通过CPU而是通过DMA直接发送，如果使用多线程，那么视觉上来讲，刷屏应该就会快一些，因为CPU可以去执行其它任务；
2. IIC主要用来跟Back板各个传感器进行通信，传感器都挂在一个总线上的；
3. TIM主要是提供时基，另外一个就是给LCD调节背光；
4. ADC只接了一个电池的分压，进行电池电压采样，预估剩余电量；
5. USART接了蓝牙，方便进行IAP和与手机和电脑的助手通信

DMA + SPI：高性能刷屏组合
✨ DMA自动搬运显存数据到SPI发送缓冲区，全程无需CPU参与
✨ CPU可以在刷屏的同时处理按键、传感器数据，系统响应更快
✨ 配合FreeRTOS多线程，可以实现流畅的动画和界面切换
✨ 这是高端智能手表保证流畅度的标配设计
 
I2C总线：传感器互联中枢
🔗 两根信号线即可挂载多个传感器，节省IO资源
🔗 统一的通信协议，驱动开发更简单
🔗 可以随时扩展新传感器，硬件设计一次成型
🔗 标准的设备地址分配，避免通信冲突
 
定时器：系统的精准节拍器
⏱️ 提供系统时钟：给FreeRTOS提供时间基准，保证任务精准调度
⏱️ PWM调光：输出不同占空比的PWM信号，实现LCD背光亮度调节
⏱️ 还可以用来实现定时采样传感器、精准延时等功能
 
ADC：精准的电量管家
🔋 通过分压电路把电池电压降到ADC可测量范围
🔋 多次采样取平均值，提高测量精度
🔋 建立电压‑电量曲线，实现精准的电量百分比显示
🔋 低电压检测，触发低电量提醒和自动关机
 
USART蓝牙：无线互联通道
📡 实现手表和手机的无线通信
📡 支持IAP空中升级，不用拆机即可更新固件
📡 同步手机通知、传输运动健康数据
📡 调试阶段可以通过蓝牙打印调试信息，方便开发

WDOG采用外置的原因是，想要做睡眠低功耗，那么使用MCU内部的看门狗关闭不了，只能一直唤醒喂狗，否则就要重启，那么这样就失去了睡眠的意义了；

---------------------------------
# 先看骨架:梳理工程框架，每层文件干什么，每个文件干什么。
![[Pasted image 20260819190508.png]]
![[Pasted image 20260819190424.png]]
## BSP（13 个子文件夹 — 板级驱动）

### 1. `IIC/` — 软件模拟 I2C 总线

|文件|作用|
|---|---|
|`iic_hal.c` + `iic_hal.h`|**软件 I2C 驱动**。用 GPIO 模拟 I2C 时序（SCL/SDA 翻转），提供 `IIC_Write_One_Byte()`、`IIC_Read_Multi_Byte()` 等函数。关键设计：用 `iic_bus_t` 结构体封装 SDA/SCL 引脚，**一条总线可以传给不同传感器用**。|

> AHT21、EM7028、SPL06、LSM303、BL24C02、CST816 都共用这套 I2C 底层。

### 2. `AHT21/` — 温湿度传感器

|文件|作用|
|---|---|
|`AHT21.c` + `AHT21.h`|温湿度传感器驱动。`AHT_Init()` 初始化、`AHT_Read(&humi, &temp)` 一次读取温度和湿度。通过 I2C 通信。|

### 3. `EM7028/` — 心率/血氧传感器（最复杂的驱动，8 个文件）

|文件|作用|
|---|---|
|`em70x8.c` + `em70x8.h`|**寄存器级驱动**。初始化 PPG 传感器、使能/失能、读取 HRS1 通道原始 ADC 数据（就是光学心率信号）。|
|`HrAlgorythm.c` + `HrAlgorythm.h`|**作者自写的心率算法**。`HR_Calculate(present_data, present_time)` 用峰值检测法算心率值。README 里说"官方库太慢，自己写了个简易算法"。|
|`user_Queue.c` + `user_Queue.h`|**数据结构：循环队列**。滑动窗口存最近的 PPG 采样点，供心率算法做峰值检测时用。|
|`HeartRatelib.lib` + `libBp.lib` + `libBpm.lib`|**预编译心率/血压算法库**（二进制 `.lib` 文件）。可能是厂商提供的官方算法，作者最终没用官方库而是自己写了 `HrAlgorythm.c`。|

### 4. `MPU6050/` — 六轴陀螺仪加速度计（8 个文件）

|文件|作用|
|---|---|
|`mpu6050.c` + `mpu6050.h`|**传感器驱动**。初始化、读取加速度/角速度/温度、设置量程和采样率。关键是做了**低功耗改造**（README 里说 DMP 库初始化后功耗很高，作者改了才能降到待机 1mA）。还有 `MPU_Sleep()` / `MPU_Wakeup()` 睡眠模式切换。|
|`eMPL/inv_mpu.c` + `inv_mpu.h`|**InvenSense 官方运动驱动库**。管理传感器配置和数据融合。|
|`eMPL/inv_mpu_dmp_motion_driver.c` + `.h`|**DMP 固件驱动**。将 DMP 固件加载到 MPU6050 内部，让芯片硬件加速计算姿态角。|
|`eMPL/dmpKey.h`|DMP 固件密钥表（初始化 DMP 寄存器用）。|
|`eMPL/dmpmap.h`|DMP 寄存器地址映射表。|

> 计步和抬腕唤醒靠它。但作者发现 DMP 中断唤醒需要抖动幅度太大，最后改用 RTC 定时检测姿态。

### 5. `LSM303DLH/` — 三轴磁力计 + 加速度计

|文件|作用|
|---|---|
|`LSM303.c` + `LSM303.h`|**指南针芯片驱动**。芯片内含磁力计（测地磁场方向）和加速度计（测重力方向）。`LSM303_ReadMagnetic()` 读磁场、`LSM303_ReadAcceleration()` 读加速度、`Azimuth_Calculate()` 计算罗盘方位角。还有 `LSM303DLH_Sleep()` / `Wakeup()`。|

> 用于指南针功能。注意它和 MPU6050 都有加速度计，各管各的。

### 6. `SPL06_001/` — 气压温度传感器

|文件|作用|
|---|---|
|`SPL06_001.c` + `SPL06_001.h`|**气压计驱动**。`Pressure_Calculate()` 算气压（hPa）、`Altitude_Calculate()` 算海拔高度、`Temperature_Calculate()` 算温度。需要先读取芯片出厂校准系数（COEF_C0~C30 那些寄存器）做补偿计算。有 `SPL_Sleep()` / `SPL_Wakeup()`。|

> 用于海拔/气压测量。校准系数读取比较复杂，头文件里那一堆 `COEF_C00_H` 之类的就是校准寄存器。

### 7. `BL24C02/` — EEPROM 存储（4 个文件）

|文件|作用|
|---|---|
|`BL24C02.c` + `BL24C02.h`|**EEPROM 硬件层驱动**。`BL24C02_Write(addr, len, buf)` / `BL24C02_Read(addr, len, buf)` 按地址读写 256 字节 EEPROM。I2C 通信。|
|`DataSave.c` + `DataSave.h`|**存储逻辑层封装**。在 BL24C02 之上封装了 `SettingSave(buf, addr, length)` 和 `SettingGet(buf, addr, length)`，给上层调用。还有 `EEPROM_Check()` 检查是否首次使用。|

> 保存用户设置（亮度、时间格式等），掉电不丢。

### 8. `LCD/` — 屏幕驱动（7 个文件）

|文件|作用|
|---|---|
|`lcd_init.c` + `lcd_init.h`|**ST7789 底层驱动**。GPIO 初始化（模拟 SPI 时序）、发送命令/数据、设坐标窗口 `LCD_Address_Set()`、屏幕初始化序列 `LCD_Init()`、背光控制 `LCD_Set_Light()`、睡眠进/出 `LCD_ST7789_SleepIn/Out()`。|
|`lcd.c` + `lcd.h`|**高层绘图接口**。`LCD_Color_Fill()` 用 DMA 批量刷屏（OV_Watch 版有，IAP 版注释掉了）、`LCD_Set_Flush_Complete_Callback()` 设置 DMA 传输完成回调（通知 LVGL 可以画下一帧）、`HAL_SPI_TxCpltCallback()` DMA 中断回调。|
|`lcdfont.h`|**字库数据**。ASCII 字符的点阵数据（C 数组），供 `LCD_ShowChar()` 用。|
|`pic.h`|**图片资源**。界面用到的图标/图片的 C 数组形式。|
|`ST7789.txt`|屏幕规格参考文档（空文件或说明）。|

> 分两层设计：`lcd_init` 管硬件时序，`lcd` 管绘图和 DMA 加速。LVGL 通过 `LCD_Color_Fill` + 回调实现异步刷屏。

### 9. `TOUCH/` — 触摸屏驱动

|文件|作用|
|---|---|
|`CST816.c` + `CST816.h`|**CST816 触控芯片驱动**。I2C 通信。`CST816_Get_XY_AXIS()` 读触摸坐标、手势识别（上/下/左/右滑、单击、双击、长按）。还有睡眠/唤醒、自动休眠时间配置、中断配置等。|

### 10. `KEY/` — 按键驱动

|文件|作用|
|---|---|
|`key.c` + `key.h`|**两个物理按键扫描**。KEY1（上键）+ KEY2（下键）。OV_Watch 版：按下+松开才触发（防误触），用 `osDelay()` 配合 FreeRTOS；IAP 版：简化逻辑用 `delay_ms()`。|

### 11. `KT6328/` — 蓝牙模块控制

|文件|作用|
|---|---|
|`KT6328.c` + `KT6328.h`|**蓝牙模块电源使能控制**。只有 3 个函数：`KT6328_GPIO_Init()` 初始化使能引脚、`KT6328_Enable()` 开蓝牙、`KT6328_Disable()` 关蓝牙。**不涉及蓝牙协议本身**，蓝牙通信走 USART 外设，协议是模块自带的 SPP 透传。|

> 不用蓝牙时直接关了省电。两个工程里这个文件一模一样。

### 12. `POWER/` — 电源管理

|文件|作用|
|---|---|
|`power.c` + `power.h`|**电池检测 + 充电检测 + 系统供电控制**。`BatCheck()` / `BatCheck_8times()` ADC 读电池电压（8 次取平均）、`ChargeCheck()` 检测是否在充电、`Power_Enable()` / `Power_DisEnable()` 控制 TPS63020 DCDC 开关（关机时断 3V3）。|

### 13. `OWDG/` — 独立看门狗

|文件|作用|
|---|---|
|`WDOG.c` + `WDOG.h`|**独立看门狗（IWDG）封装**。`WDOG_Enable()` 开启、`WDOG_Feed()` 喂狗（必须在超时前调用，否则系统复位重启）、`WDOG_Disnable()` 关闭。|

> 防止程序跑飞死机。跑 RTOS 时一般每个任务循环里喂一次。

-----------------------------
## IAP OTA YMODEM
**IAP：** 这是升级的能力，全称In‑Application Programming(在线应用编程)，指设备可以在不拆机的情况下自己给自己升级程序，是一种升级方式。
**OTA：** 这是升级的传输方式，全称Over‑The‑Air（空中升级），指通过无线方式升级，比如蓝牙、WiFi、蜂窝网络，不用插线就能升级。
**YMODEM：** 这是传输协议，负责把固件文件准确地从电脑或手机传到设备里，保证传输过程中不会出错。
 
**它们的关系就像是快递系统** 
‑ **IAP就是你家小区的快递柜** ，提供了接收快递的能力，不管快递是怎么送过来的，都可以存在这里。
‑ **OTA就是无人机送货** ，是一种无线的送货方式，不用快递员上门，直接飞到你家窗户边把快递递给你。
‑ **YMODEM就是快递的物流标准** ，规定了快递怎么打包、怎么验货、怎么签收，保证快递不会丢件不会损坏。
 
实际升级中的完整流程
1. 第一步：通过OTA传输：手机通过蓝牙把固件包无线发送给手表，这就是OTA升级。
2. 第二步：用YMODEM保证可靠传输：传输过程中使用YMODEM协议，把固件分成1KB一包，每包都加校验，确保固件不会传错。
3. 第三步：IAP完成升级：手表的Bootloader收到完整的固件包后，把它写入Flash的APP分区，完成升级并启动新程序。
 
它们不是互斥关系，而是互补关系IAP是基础能力，OTA是传输方式，YMODEM是传输协议。你可以用YMODEM配合串口线做有线IAP升级，也可以用YMODEM配合蓝牙做OTA无线升级，它们可以自由组合，适配不同的升级场景。









